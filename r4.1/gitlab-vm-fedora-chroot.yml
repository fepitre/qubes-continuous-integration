chroot:vm-fc37:
  extends: .chroot_prepare
  needs:
    - prep:sources
  variables:
    DISTS_VM: fc37
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

chroot:vm-fc37-legacy:
  extends: .chroot_prepare
  tags:
    - docker
  needs:
    - prep:sources
  variables:
    USE_DIST_BUILD_TOOLS: 0
    DISTS_VM: fc37
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

chroot:vm-fc38:
  extends: .chroot_prepare
  needs:
    - prep:sources
  variables:
    DISTS_VM: fc38
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

chroot:vm-fc38-legacy:
  extends: .chroot_prepare
  tags:
    - docker
  needs:
    - prep:sources
  variables:
    USE_DIST_BUILD_TOOLS: 0
    DISTS_VM: fc38
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"
