build:vm-fc34:
  extends: .components_build
  needs:
    - prep:sources
  variables:
    DISTS_VM: fc34
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

build:vm-fc35:
  extends: .components_build
  needs:
    - prep:sources
  variables:
    DISTS_VM: fc35
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

build:vm-fc36:
  extends: .components_build
  needs:
    - prep:sources
  variables:
    DISTS_VM: fc36
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"
