build:vm-fc37:
  extends: .components_build
  needs:
    - prep:sources
    - chroot:vm-fc37
  variables:
    DISTS_VM: fc37
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"

build:vm-fc38:
  extends: .components_build
  needs:
    - prep:sources
    - chroot:vm-fc38
  variables:
    DISTS_VM: fc38
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"
